export interface TimeInterval {
  since: Date;
  to: Date;
}

export interface CircleDistance {
  latitude: number;
  longitude: number;
  distance: number;
}

export interface MagnitudeRange {
  minimum: number;
  maximum: number;
}
